import 'package:flutter/material.dart';

class TopicClusters extends StatelessWidget {
  const TopicClusters({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Topic Clusters Section (Coming Soon)"));
  }
}
