{
  "name": "article-builder-front",
  "private": true,
  "version": "1.0.0",
  "description": "Root scripts to run mock backend and React app",
  "scripts": {
    "mock": "npm --prefix mock-backend start",
    "web": "npm --prefix react-app run dev",
    "dev": "npm run mock & npm run web"
  },
  "dependencies": {
    "@radix-ui/react-slot": "^1.2.3",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "lucide-react": "^0.537.0",
    "react-resizable-panels": "^3.0.4",
    "tailwind-merge": "^3.3.1",
    "tailwindcss": "^4.1.12"
  }
}
